services:
  ceylinco:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    env_file:
      - .env
    networks:
      - nginx-net

networks:
  nginx-net:
    external: true
