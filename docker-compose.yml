services:
  ceylinco:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    environment:
      - NODE_ENV=production
    networks:
      - nginx-net

networks:
  nginx-net:
    external: true
